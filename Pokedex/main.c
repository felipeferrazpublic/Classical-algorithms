#include <stdio.h>
#include <stdlib.h>
#include "lista.h"

int main(){
/*
    int i;
    int x;
    float y;
    List *li;

    li = cria_lista(sizeof(int));

    for(i = 0; i < 10; i++){
        printf("Digite um numero: ");
        scanf("%d", &x);
        fflush(stdin);
        add_list_sort(li, &x, x);
    }

    puts("\n");
    print_lista(li);
*/
    return 0;
}
